name: Compilation des PDS

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "compte-rendus/CAA-FR_compte-rendu_*.md"
      - ".github/workflows/build-meeting-pdfs.yml"

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    container:
      image: pandoc/latex:latest-ubuntu
      options: --user root
    defaults:
      run:
        shell: bash
    permissions:
      contents: write

    steps:
      - name: Installation Linux Libertine
        run: |
          apt-get update
          apt-get install -y --no-install-recommends fonts-linuxlibertine git ca-certificates
          fc-cache -f
     # update du containter necessaire pour pouvoir installer git

      - name: Check out main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

     # On recupere la compte rendu modifié en derniere et on compile
      - name: Build PDF
        run: |
          mkdir -p pdf
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

          meeting_file="$(git log -1 --format= --name-only --diff-filter=AM -- 'compte-rendus/CAA-FR_compte-rendu_*.md' | head -n 1)"
          pdf_file="pdf/$(basename "${meeting_file%.md}").pdf"

          echo "Construction: $meeting_file"
          echo "Chemin stockage: $pdf_file"

          pandoc "$meeting_file" --pdf-engine=xelatex -o "$pdf_file"

      - name: ajoud du fichier compilé dans l'espace de travail
        uses: actions/upload-artifact@v4
        with:
          name: meeting-pdf
          path: pdf/*.pdf

      - name: Ajout du fichier dans la branche 'pdf'
        if: github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          publish_dir="$RUNNER_TEMP/pdf-branch"
          mkdir -p "$publish_dir"
          cp pdf/*.pdf "$publish_dir"/

          cd "$publish_dir"

          git init
          git checkout -b pdf
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Update compiled meeting PDF"

          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin pdf
